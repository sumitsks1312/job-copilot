services:
  app:
    build: .
    ports:
      - "80:5000"
    volumes:
      # Persists database.db and uploads/ on the host under ./data/
      - ./data:/data
    env_file:
      - .env
    restart: unless-stopped
